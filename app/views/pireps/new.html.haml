= content_for :section, 'File PIREP'

.container.pt-2.pb-4
  .col-lg-12
    .card.card-shadow
      .card-header
        %h5.pt-2= 'New PIREP'
      .card-body
        = render partial: 'form', locals: { target: @pirep, options: {} }
