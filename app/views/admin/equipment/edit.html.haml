= content_for :section, 'Equipment Type Administration'

= content_for :card_header do
  .col-8.pt-1
    %h5= 'Edit Equipment Type'
  .col-4.text-right
    = link_to admin_equipment_path(@equipment), method: :delete, class: 'btn btn-sm btn-danger', 'data-confirm': "Are you sure?" do
      %i.fas.fa-trash
      Delete

= render 'admin/layout' do
  .p-4
    = render partial: 'form'
